apiVersion: v1
kind: Service

metadata:
  name: production-app-service
  namespace: production

spec:
  selector:
    app: production-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: ClusterIP
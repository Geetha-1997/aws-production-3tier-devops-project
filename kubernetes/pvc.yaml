apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: production-app-pvc
  namespace: production

spec:
  accessModes:
    - ReadWriteOnce

  storageClassName: standard

  resources:
    requests:
      storage: 1Gi
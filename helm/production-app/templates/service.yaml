apiVersion: v1
kind: Service

metadata:
  name: production-app

spec:
  type: {{ .Values.service.type }}

  selector:
    app: production-app

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP